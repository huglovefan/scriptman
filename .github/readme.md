# scriptman

## building

install dependencies:

```
npm install
```

minified build:

```
npm run build
```

unminified build + watch mode:

```
npm run build-dev
```

`./dist/` is where the results go